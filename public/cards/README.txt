# Card Artwork

Drop card thumbnails referenced by `data/apps.json` here. Filenames should remain kebab-case (e.g., `creative-city.png`). Recommended size: 640x360 PNG with transparent background.

Use `python3 scripts/generate_card_art.py` to regenerate the current bespoke artwork set or to iterate on the vector-based thumbnails.
