# Texture Assets

- `codeverse_map.png`: Base isometric map baked from design tools.
- `codeverse_map_height.png`: Optional grayscale height map enabling displacement mode (set `VITE_USE_HEIGHTMAP=true`).

Replace these with updated renders when the world layout changes.
